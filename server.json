{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.payclaw/badge",
  "description": "Prove your agent is an authorized actor, not a bot. MCP-native identity for agent commerce.",
  "repository": {
    "url": "https://github.com/payclaw/badge-server",
    "source": "github"
  },
  "version": "0.7.3",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@payclaw/badge",
      "version": "0.7.3",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "PAYCLAW_API_KEY",
          "description": "Optional. Your PayClaw API key for existing accounts. If omitted, device authorization flow triggers automatically on first use.",
          "isRequired": false,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "PAYCLAW_API_URL",
          "description": "PayClaw API URL (default: https://payclaw.io)",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "PAYCLAW_EXTENDED_AUTH",
          "description": "Set to \"true\" or \"1\" to enable. Asks your agent to confirm merchant accepted or denied. Logged to dashboard so you can see visibility of your token by merchant. Default: false (agent reports via payclaw_reportBadgeOutcome).",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        }
      ]
    }
  ]
}
