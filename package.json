{
  "name": "badge-sdk-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/sdk",
    "packages/mcp"
  ],
  "scripts": {
    "build": "npm run build -w packages/sdk && npm run build -w packages/mcp",
    "test": "npm run test -w packages/sdk -- run && npm run test -w packages/mcp -- run",
    "lint": "npm run lint -w packages/sdk && npm run lint -w packages/mcp"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "typescript": "^5.3.0",
    "typescript-eslint": "^8.59.0"
  }
}
